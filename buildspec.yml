version: 0.1

phases:
  build:
    install:
      runtime - versions:
      n 16.13.0  
    commands:
      - npm install
      - npm run webpack

artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
    - dist/bundle.js
